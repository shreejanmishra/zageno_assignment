{
  "name": "zageno-assignment-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0",
    "mongoose": "^9.3.3",
    "node-cache": "^5.1.2",
    "zod": "^3.24.2"
  }
}
